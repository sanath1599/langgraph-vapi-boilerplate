# Backend API base. Dev: use /api (Vite proxies /api to backend; frontend routes don't conflict).
# Production: set your backend URL, e.g. https://api.example.com
VITE_API_BASE=/api
VITE_DEFAULT_CALLER=+15855652555
VITE_LLM_SERVER_URL=http://localhost:6065